{
  "name": "spannungsatlas",
  "version": "0.1.0",
  "private": true,
  "description": "Pädagogisches Reflexions- und Dokumentationssystem",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  }
}
