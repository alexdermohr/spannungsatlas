import {
  fromCatalogSelections,
  toCatalogSelections,
  toggleSelectionId,
} from './exploration-selection-model.js';

export { toCatalogSelections, toggleSelectionId };

export const selectedIdsFromCatalogSelections = fromCatalogSelections;
